public class ConceitoDefinicao {
    // Todo método deve estar dentro de uma classe

    /*
    <?visibilidade?><?tipo?><?modificador?>retorno nome(<?parâmetros?>)<?exceções?>corpo

     * Visibilidade: "public", "protected" ou "private". (mais usado)
     * Tipo: concreto ou abstrato. 
     * Modificador: "static" ou final. 
     * Retorno: tipo de dado ou "void". (mais usado)
     * Nome: nome que é fornecido ao método. (mais usado)
     * Parâmetros: parâmetros que pode lançar. (mais usado)
     * Exceções: exceções que pode lançar. 
     * Corpo: código que possui ou vazio. (mais usado)
     */
}

// public static Retorno Nome (parâmetro){}

/*
* Chamando por meio da classe:
* nome_da_classe.nome_do_metodo();
* Math.random(); ou Math.sqrt(4);
* 

* Chamando por meio do objeto:
* nome_do_objeto.nome_do_metodo();
* usuario.getEmail(); ou usuario.alterarEndereco(enderco);

*/