public class Sobrecarga {
    
    // É a capacidade de definir métodos para diferentes contextos, mas preservando seu nome. Mantemos o nome e mudamos a lista de parâmetros. 

    /*Quanto mais complexa a aplicação mais complexas ficam as sobrecargas. 

    Alterar a assinatura do método:
    Ass = nome + parâmetros
    converterParaInteiro (float f);
    converterParaInteiro(double d);
    converterParaInteiro(float f, RoundType rd);
    converterParaInteiro(double d, RoundType rd);
    converterParaInteiro(String s, RoundType rd);
    converterParaInteiro(RoundType rd, String s);

    converterParaInteiro();


    
    Sobrecarga x sobrescrita

    A sobrecarga precisa ter a herança (orientação a  objeto). Ambas atuam sobre o metodo, mas atuam de forma diferente. 
    */
}
